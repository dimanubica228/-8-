fun findMax(num1: Int, num2: Int): Int {
    return if (num1 > num2) {
        num1
    } else {
        num2
    }
}

fun main() {
    val number1 = 10
    val number2 = 5

    val maxNumber = findMax(number1, number2)

    println("Максимальное число: $maxNumber")
}
