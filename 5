fun main() {
    val prob = 0.5 // 50% шанс выиграть
    val prize = 100.0 // Приз в 100 долларов
    val pay = 50.0 // Стоимость участия 50 долларов

    val isWorthIt = isWorthIt(prob, prize, pay) // isWorthIt будет равен true

    println(isWorthIt) // Выведет true, так как 0.5  100 > 50
}

fun isWorthIt(prob: Double, prize: Double, pay: Double) {

}
